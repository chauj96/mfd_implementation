[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "adaptive-mfd-solver"
version = "0.1.0"
dependencies = [
    "numpy",
    "scipy",
    "matplotlib",
    "scikit-umfpack"
]
